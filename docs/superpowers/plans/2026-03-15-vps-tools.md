# VPS Tools 实现计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `cf_test.sh` 扩展为交互式多功能 VPS 工具箱 `vps_tools.sh`

**Architecture:** 单文件 Bash 脚本，顶部公共函数 + 各功能模块函数 + 底部菜单循环入口。现有测速代码封装为函数，新增 SSH 加固、系统更新、探针安装、质量检测、Snell 安装五个功能模块。

**Tech Stack:** Bash, curl, awk, sed, systemctl, ufw

**Spec:** `docs/superpowers/specs/2026-03-15-vps-tools-design.md`

---

## Chunk 1: 脚手架 — 重命名、菜单框架、测速封装

### Task 1: 重命名文件并搭建菜单框架

**Files:**
- Rename: `cf_test.sh` → `vps_tools.sh`

- [ ] **Step 1: 重命名文件**

```bash
git mv cf_test.sh vps_tools.sh
```

- [ ] **Step 2: 替换脚本顶部，添加菜单框架**

将 `vps_tools.sh` 改造为以下结构（保留原有所有函数，在文件末尾替换执行逻辑为菜单循环）：

脚本顶部保持 `#!/usr/bin/env bash` 和 `set -euo pipefail`。

在颜色设置之后、现有函数之前，添加 root 检测函数：

```bash
# ---- Root 权限检测 ----
check_root() {
  if [[ "$EUID" -ne 0 ]]; then
    echo -e "${C_CYAN}提示：当前非 root 用户，部分功能需要 root 权限${C_RESET}"
    return 1
  fi
  return 0
}

require_root() {
  if ! check_root 2>/dev/null; then
    echo -e "\n${C_CYAN}此功能需要 root 权限，请使用 sudo 或以 root 用户运行${C_RESET}"
    return 1
  fi
}
```

- [ ] **Step 3: 将现有测速执行逻辑封装为 `do_speedtest()` 函数**

将文件末尾第 483-524 行的执行逻辑（从 `TIMESTAMP=` 到 `render_html`）包裹在函数中：

```bash
do_speedtest() {
  local MEAS_ID
  MEAS_ID="$(date +%s%N)"

  local tmpdir
  tmpdir="$(mktemp -d)"
  trap 'rm -rf "$tmpdir"' EXIT  # 注意：在函数内需要管理 trap

  RESULTS_FILE="${tmpdir}/results.dat"

  # （原有第 483-524 行的全部逻辑搬入此处）
  TIMESTAMP="$(date '+%Y-%m-%d %H:%M:%S')"
  write_result "MEAS_ID" "$MEAS_ID"
  write_result "TIMESTAMP" "\"$TIMESTAMP\""
  write_result "TARGET" "$BASE"
  write_result "IPV4_FORCED" "yes"

  lat_idle="${tmpdir}/lat_idle.txt"
  latency_collect "${BASE}/__down?bytes=0&measId=${MEAS_ID}" "$LAT_SAMPLES" "$LAT_INTERVAL" "$lat_idle"
  latency_summary "$lat_idle" "IDLE_LAT" "idle latency"

  if [[ "$MEASURE_LOADED_LATENCY" == "1" ]]; then
    lat_d="${tmpdir}/lat_dload.txt"
    latency_collect "${BASE}/__down?bytes=0&during=download&measId=${MEAS_ID}" "$LAT_SAMPLES" "$LAT_INTERVAL" "$lat_d" &
    pid_d=$!
  fi

  download_test "$DL_BYTES" "$DL_N"

  if [[ "$MEASURE_LOADED_LATENCY" == "1" ]]; then
    wait "$pid_d" || true
    latency_summary "$lat_d" "DL_LAT" "download latency"
  fi

  if [[ "$MEASURE_LOADED_LATENCY" == "1" ]]; then
    lat_u="${tmpdir}/lat_uload.txt"
    latency_collect "${BASE}/__down?bytes=0&during=upload&measId=${MEAS_ID}" "$LAT_SAMPLES" "$LAT_INTERVAL" "$lat_u" &
    pid_u=$!
  fi

  upload_test

  if [[ "$MEASURE_LOADED_LATENCY" == "1" ]]; then
    wait "$pid_u" || true
    latency_summary "$lat_u" "UP_LAT" "upload latency"
  fi

  render_terminal
  render_html
}
```

同时需要将原先在文件顶层的 `tmpdir`、`MEAS_ID`、`RESULTS_FILE` 等变量初始化移入函数内部（或移除顶层的），使函数自包含。顶层的 `trap` 和 `tmpdir` 定义（第 35-38 行）也应移入 `do_speedtest()` 内。

- [ ] **Step 4: 添加菜单循环作为脚本入口（替换原第 483-524 行的直接执行代码）**

在文件末尾添加：

```bash
# ============================================================
#  主菜单
# ============================================================
show_menu() {
  echo ""
  echo -e "${C_CYAN}=========================================${C_RESET}"
  echo -e "${C_BOLD_WHITE}         VPS Tools v1.0${C_RESET}"
  echo -e "${C_CYAN}=========================================${C_RESET}"
  echo " 1) SSH 安全加固"
  echo " 2) 系统更新"
  echo " 3) 安装哈探针 (Nezha Agent)"
  echo " 4) 服务器质量检测 (NodeQuality)"
  echo " 5) Snell 安装"
  echo " 6) Cloudflare 测速"
  echo " 0) 退出"
  echo -e "${C_CYAN}=========================================${C_RESET}"
}

main() {
  while true; do
    show_menu
    read -rp "请输入选项 [0-6]: " choice
    echo ""
    case "$choice" in
      1) require_root && do_ssh_harden ;;
      2) require_root && do_system_update ;;
      3) require_root && do_nezha_install ;;
      4) do_node_quality ;;
      5) require_root && do_snell_install ;;
      6) do_speedtest ;;
      0) echo "再见！"; exit 0 ;;
      *) echo "无效选项，请重新输入" ;;
    esac
  done
}

main
```

- [ ] **Step 5: 验证语法**

```bash
bash -n vps_tools.sh
```

预期：无输出（语法正确）。此时 `do_ssh_harden` 等函数尚未定义，但 `bash -n` 仅检查语法不执行。

- [ ] **Step 6: 提交**

```bash
git add vps_tools.sh
git commit -m "refactor: rename cf_test.sh to vps_tools.sh, add menu framework and wrap speedtest in function"
```

---

## Chunk 2: SSH 安全加固

### Task 2: 实现 SSH 安全加固函数

**Files:**
- Modify: `vps_tools.sh`（在 `do_speedtest()` 函数之前添加 `do_ssh_harden()` 函数）

- [ ] **Step 1: 添加 `do_ssh_harden()` 函数**

在 `do_speedtest()` 函数之前插入：

```bash
# ============================================================
#  1) SSH 安全加固
# ============================================================
do_ssh_harden() {
  echo -e "${C_CYAN}=== SSH 安全加固 ===${C_RESET}"

  # --- 1. 获取新端口 ---
  local new_port
  read -rp "请输入新的 SSH 端口 [默认 2222]: " new_port
  new_port="${new_port:-2222}"

  # 端口范围校验
  if ! [[ "$new_port" =~ ^[0-9]+$ ]] || (( new_port < 1 || new_port > 65535 )); then
    echo "端口无效，请输入 1-65535 之间的数字"
    return 1
  fi

  # --- 2. 获取公钥 ---
  echo ""
  echo "请粘贴你的 SSH 公钥（以 ssh-rsa / ssh-ed25519 / ecdsa-sha2 开头）："
  local pubkey
  read -r pubkey

  # 格式校验
  if ! [[ "$pubkey" =~ ^(ssh-rsa|ssh-ed25519|ecdsa-sha2|ssh-dss)[[:space:]] ]]; then
    echo "公钥格式无效，请检查后重试"
    return 1
  fi

  # --- 3. 写入 authorized_keys ---
  local ssh_dir="$HOME/.ssh"
  mkdir -p "$ssh_dir"
  chmod 700 "$ssh_dir"

  if grep -qF "$pubkey" "$ssh_dir/authorized_keys" 2>/dev/null; then
    echo "该公钥已存在于 authorized_keys 中，跳过写入"
  else
    echo "$pubkey" >> "$ssh_dir/authorized_keys"
    chmod 600 "$ssh_dir/authorized_keys"
    echo "公钥已写入 $ssh_dir/authorized_keys"
  fi

  # --- 4. 备份并修改 sshd_config ---
  local sshd_config="/etc/ssh/sshd_config"
  local backup="${sshd_config}.bak.$(date +%Y%m%d%H%M%S)"
  cp "$sshd_config" "$backup"
  echo "已备份 sshd_config → $backup"

  # 使用 sed 修改配置（先去注释再替换）
  sed -i \
    -e "s/^#\?Port .*/Port ${new_port}/" \
    -e "s/^#\?PubkeyAuthentication .*/PubkeyAuthentication yes/" \
    -e "s/^#\?PasswordAuthentication .*/PasswordAuthentication no/" \
    -e "s/^#\?PermitRootLogin .*/PermitRootLogin prohibit-password/" \
    "$sshd_config"

  # 如果配置项不存在则追加
  grep -q "^Port " "$sshd_config" || echo "Port ${new_port}" >> "$sshd_config"
  grep -q "^PubkeyAuthentication " "$sshd_config" || echo "PubkeyAuthentication yes" >> "$sshd_config"
  grep -q "^PasswordAuthentication " "$sshd_config" || echo "PasswordAuthentication no" >> "$sshd_config"
  grep -q "^PermitRootLogin " "$sshd_config" || echo "PermitRootLogin prohibit-password" >> "$sshd_config"

  echo "sshd_config 已更新：Port=${new_port}, 密钥登录, 禁用密码"

  # --- 5. Ubuntu 特殊处理：ssh.socket ---
  local socket_file="/lib/systemd/system/ssh.socket"
  if [[ -f "$socket_file" ]]; then
    echo "检测到 Ubuntu ssh.socket，同步修改端口..."
    local socket_backup="${socket_file}.bak.$(date +%Y%m%d%H%M%S)"
    cp "$socket_file" "$socket_backup"
    sed -i "s/^ListenStream=.*/ListenStream=${new_port}/" "$socket_file"
    systemctl daemon-reload
    echo "ssh.socket 已更新并重载 daemon"
  fi

  # --- 6. 配置 ufw ---
  if command -v ufw &>/dev/null; then
    echo "配置防火墙..."
    ufw allow "${new_port}/tcp" comment "SSH"
    ufw --force enable
    echo "ufw 已放行端口 ${new_port}/tcp"
  else
    echo -e "${C_CYAN}提示：未检测到 ufw，请手动配置防火墙放行端口 ${new_port}${C_RESET}"
  fi

  # --- 7. 重启 SSH 服务 ---
  if systemctl is-active --quiet sshd 2>/dev/null; then
    systemctl restart sshd
  elif systemctl is-active --quiet ssh 2>/dev/null; then
    systemctl restart ssh
  else
    echo "警告：无法确定 SSH 服务名，请手动重启 SSH 服务"
  fi

  # --- 8. 醒目提示 ---
  echo ""
  echo -e "${C_BOLD_WHITE}╔══════════════════════════════════════════════════════════╗${C_RESET}"
  echo -e "${C_BOLD_WHITE}║  ⚠  重要提示                                           ║${C_RESET}"
  echo -e "${C_BOLD_WHITE}║  请用新端口 ${new_port} 新开一个终端测试 SSH 连接          ║${C_RESET}"
  echo -e "${C_BOLD_WHITE}║  确认能正常登录后，再关闭当前会话！                      ║${C_RESET}"
  echo -e "${C_BOLD_WHITE}║  ssh -p ${new_port} root@<服务器IP>                       ║${C_RESET}"
  echo -e "${C_BOLD_WHITE}╚══════════════════════════════════════════════════════════╝${C_RESET}"
  echo ""
}
```

- [ ] **Step 2: 验证语法**

```bash
bash -n vps_tools.sh
```

- [ ] **Step 3: 提交**

```bash
git add vps_tools.sh
git commit -m "feat: add SSH hardening with port change, key auth, ufw, and Ubuntu ssh.socket support"
```

---

## Chunk 3: 系统更新 + 探针安装 + 质量检测 + Snell 安装

### Task 3: 实现系统更新函数

**Files:**
- Modify: `vps_tools.sh`（在 `do_ssh_harden()` 之后添加）

- [ ] **Step 1: 添加 `do_system_update()` 函数**

```bash
# ============================================================
#  2) 系统更新
# ============================================================
do_system_update() {
  echo -e "${C_CYAN}=== 系统更新 ===${C_RESET}"

  if command -v apt &>/dev/null; then
    echo "检测到 apt，开始更新..."
    apt update && apt upgrade -y
  elif command -v dnf &>/dev/null; then
    echo "检测到 dnf，开始更新..."
    dnf upgrade -y
  elif command -v yum &>/dev/null; then
    echo "检测到 yum，开始更新..."
    yum update -y
  else
    echo "未检测到支持的包管理器（apt/dnf/yum）"
    return 1
  fi

  echo -e "\n${C_CYAN}系统更新完成${C_RESET}"
}
```

- [ ] **Step 2: 提交**

```bash
git add vps_tools.sh
git commit -m "feat: add system update with apt/dnf/yum auto-detection"
```

### Task 4: 实现探针安装函数

- [ ] **Step 1: 添加 `do_nezha_install()` 函数**

```bash
# ============================================================
#  3) 安装哈探针 (Nezha Agent)
# ============================================================
do_nezha_install() {
  echo -e "${C_CYAN}=== 安装哈探针 (Nezha Agent) ===${C_RESET}"
  echo ""
  echo "请粘贴完整的安装命令（包含 NZ_SERVER、NZ_CLIENT_SECRET 等参数）："
  echo -e "${C_GRAY}示例: curl -L https://raw.githubusercontent.com/nezhahq/scripts/main/agent/install.sh -o agent.sh && chmod +x agent.sh && env NZ_SERVER=... NZ_TLS=true NZ_CLIENT_SECRET=... NZ_UUID=... ./agent.sh${C_RESET}"
  echo ""

  local cmd
  read -r cmd

  # 基本校验
  if [[ -z "$cmd" ]]; then
    echo "命令为空，已取消"
    return 1
  fi

  if ! echo "$cmd" | grep -q "NZ_SERVER" || ! echo "$cmd" | grep -q "NZ_CLIENT_SECRET"; then
    echo "命令中未找到 NZ_SERVER 或 NZ_CLIENT_SECRET，请检查"
    return 1
  fi

  echo ""
  echo -e "即将执行：\n${C_GRAY}${cmd}${C_RESET}"
  read -rp "确认执行？[y/N]: " confirm
  if [[ "${confirm,,}" != "y" ]]; then
    echo "已取消"
    return 0
  fi

  eval "$cmd"
  echo -e "\n${C_CYAN}探针安装命令已执行${C_RESET}"
}
```

- [ ] **Step 2: 提交**

```bash
git add vps_tools.sh
git commit -m "feat: add Nezha Agent installation with command validation"
```

### Task 5: 实现服务器质量检测函数

- [ ] **Step 1: 添加 `do_node_quality()` 函数**

```bash
# ============================================================
#  4) 服务器质量检测 (NodeQuality)
# ============================================================
do_node_quality() {
  echo -e "${C_CYAN}=== 服务器质量检测 (NodeQuality) ===${C_RESET}"
  bash <(curl -sL https://run.NodeQuality.com)
}
```

- [ ] **Step 2: 提交**

```bash
git add vps_tools.sh
git commit -m "feat: add NodeQuality server check"
```

### Task 6: 实现 Snell 安装函数

- [ ] **Step 1: 添加 `do_snell_install()` 函数**

```bash
# ============================================================
#  5) Snell 安装
# ============================================================
do_snell_install() {
  echo -e "${C_CYAN}=== Snell 安装 ===${C_RESET}"
  bash <(curl -L -s menu.jinqians.com)
}
```

- [ ] **Step 2: 提交**

```bash
git add vps_tools.sh
git commit -m "feat: add Snell installation via jinqians.com"
```

### Task 7: 最终验证与提交

- [ ] **Step 1: 语法检查**

```bash
bash -n vps_tools.sh
```

预期：无输出

- [ ] **Step 2: 检查文件结构是否合理**

确认函数顺序为：
1. shebang + set 选项
2. 公共常量（BASE, 测速参数等）
3. 颜色设置
4. `check_root()` / `require_root()`
5. `write_result()`
6. 测速辅助函数（`render_terminal`, `svg_scatter`, `render_html`, `latency_collect`, `latency_summary`, `download_test`, `upload_test`）
7. `do_ssh_harden()`
8. `do_system_update()`
9. `do_nezha_install()`
10. `do_node_quality()`
11. `do_snell_install()`
12. `do_speedtest()`
13. `show_menu()` + `main()` + `main` 调用

- [ ] **Step 3: 最终提交（如有未提交的修改）**

```bash
git add vps_tools.sh
git commit -m "chore: finalize vps_tools.sh structure"
```
